// index; starting at 0

const pets = ['cat', 'dog', 'rat']
for (let i=0; i<pets.length; i++) {
    pets[i] += 's'
}

// let pets = ['cat', 'dog', 'rat']
// pets = pets.map(pet => pet + 's')

console.log(pets)


// node 14_looping_through_arrays.js
// javascripting verify 14_looping_through_arrays.js


